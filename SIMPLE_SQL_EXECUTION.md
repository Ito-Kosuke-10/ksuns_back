# 最も簡単な方法：MySQL Workbenchを使う

Azure PortalでクエリエディターやCloud Shellが見つからない場合は、**MySQL Workbench**を使うのが最も簡単です。

## 🚀 手順

### 1. MySQL Workbenchをインストール

1. [MySQL Workbench ダウンロードページ](https://dev.mysql.com/downloads/workbench/)にアクセス
2. **Windows (x86, 64-bit), MSI Installer**をダウンロード
3. インストール（デフォルト設定でOK）

### 2. Azure MySQLに接続

1. **MySQL Workbench**を起動
2. **「+」**ボタンをクリックして新しい接続を作成
3. 以下の情報を入力：
   ```
   Connection Name: Azure MySQL
   Hostname: gen10-mysql-dev-01.mysql.database.azure.com
   Username: students
   Password: （Azure Portalで確認したパスワード）
   Default Schema: ksuns
   ```
4. **「Test Connection」**をクリック
   - 接続に成功すれば「Successfully made the MySQL connection」と表示されます
5. **「OK」**をクリック

### 3. SQLを実行

1. 作成した接続を**ダブルクリック**して接続
2. **「File」→「Open SQL Script」**をクリック
3. **`create_tables.sql`** ファイルを選択して開く
4. **実行ボタン**（⚡アイコン、またはF5キー）をクリック
5. 成功メッセージが表示されれば完了！

---

## 📋 接続情報の確認方法

Azure Portalで接続情報を確認するには：

1. Azure Database for MySQLのページで、左側メニューの**「接続」**をクリック
2. 以下の情報が表示されます：
   - **サーバー名**: `gen10-mysql-dev-01.mysql.database.azure.com`
   - **管理者のログイン**: `students`
   - **パスワード**: （表示されない場合は、リセットが必要）

---

## ⚠️ ファイアウォール設定

接続できない場合は、ファイアウォール設定を確認：

1. Azure Database for MySQLのページで、左側メニューの**「ネットワーク」**をクリック
2. **「パブリック アクセス」**タブを確認
3. 現在のIPアドレスを追加するか、**「すべてのAzureサービスからのアクセスを許可」**を有効にする

---

## 💡 その他の方法

### 方法2: Azure Portal右上のCloud Shellアイコン

1. Azure Portalの**右上**を確認
2. **`>_`** のようなアイコン（Cloud Shell）を探す
3. クリックして開く
4. 以下のコマンドでMySQLに接続：
   ```bash
   mysql -h gen10-mysql-dev-01.mysql.database.azure.com -u students -p ksuns
   ```

### 方法3: コマンドプロンプトから（MySQLクライアントがインストールされている場合）

```cmd
mysql -h gen10-mysql-dev-01.mysql.database.azure.com -u students -p ksuns
```

その後、`create_tables.sql`の内容を貼り付けて実行。

---

**最も簡単で確実なのは、MySQL Workbenchを使う方法です！** ✅



